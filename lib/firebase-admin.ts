/** Compatibility re-export — prefer `@/lib/firebase/admin`. */
export {
  verifyFirebaseIdToken,
  createFirebaseSession,
  verifyFirebaseSession,
  deleteFirebaseUser,
} from "@/lib/firebase/admin";
