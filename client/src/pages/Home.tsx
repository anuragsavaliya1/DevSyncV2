/**
 * Quiet Command Center design system: DevSync's white operational surface is rendered
 * as a persistent command rail with contextual, action-oriented workspace views.
 */
import DevSyncDashboard from "@/components/DevSyncDashboard";

export default function Home() {
  return <DevSyncDashboard />;
}
